{
  "name": "ps5upload-app",
  "private": true,
  "version": "1.5.4",
  "description": "PS5Upload remote app service (backend + frontend)",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "bench:buffer": "node ../scripts/bench-buffer-readers.js --iterations 9 --warmup 2 --mb 256 --frame 4096"
  },
  "dependencies": {
    "hash-wasm": "^4.12.0",
    "undici": "^6.21.2"
  }
}
